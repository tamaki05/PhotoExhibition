<div class=" d-flex align-items-center justify-content-center pt-5">
  <div class="container">
    <div class="row">
      <div class="col-11 col-md-8 col-lg-6 mx-auto">
        <h4 class="text-center"><strong>Exhibition</strong></h4><br>
        <p class="text-center">PhotoExhibitionでは<br>
                              「風景」「動物」「人物」「アート」の<br>
                              4つのテーマに分けて公開しています。<br>
                              ぜひギャラリーをご覧ください！
        </p>
      </div>
    </div><br><br>
    <div class="text-center">
      <div class="zoomIn"><a href="#"><span class="mask">
        <% @categories.each do |category| %>
        <li>「<%= link_to category.name, category_path(category.id), class: "link-black" %>」</li><br>

            <!--カテゴリー別に写真をつける-->
            <% if category.id == @category_first_index  %>
              <%= link_to image_tag("huukei.jpg", size: "500x200"), category_path(category.id) %><br><br><br>
            <% end %>
            <% if category.id == (@category_first_index + 1)  %>
              <%= link_to image_tag("doubutu.jpg", size: "500x200"), category_path(category.id) %><br><br><br>
            <% end %>
            <% if category.id == (@category_first_index + 2)  %>
              <%= link_to image_tag("jinnbutu.jpg", size: "500x200"), category_path(category.id) %><br><br><br>
            <% end %>
            <% if category.id == (@category_first_index + 3)  %>
              <%= link_to image_tag("art.jpg", size: "500x200"), category_path(category.id) %><br><br><br>
            <% end %>
         <% end %>
      </span></a></div>
    </div>
  </div>
</div>


