<div class=" d-flex align-items-center justify-content-center pt-5">
  <div class="container">
    <div class="row">
      <div class="col-lg-8">
        <p>メッセージ一覧</p>
        <% @rooms.each do |room| %>
          <%# if message.user_id == current_user.id %>
          <%# @user.name %>
        <% end %>
      </div>
    </div>
  </div>
</div>